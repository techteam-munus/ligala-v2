// shadcn/ui components added via `pnpm dlx shadcn@latest add <component>` from
// inside apps/web (they land in apps/web/components/ui per components.json).
// Genuinely shared primitives — design tokens, logo, layout shells used in
// more than one app — live in this package.

export { cn } from "./cn.js";
