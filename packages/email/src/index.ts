// React Email templates live under ./templates and are rendered to HTML +
// plaintext via @react-email/render, then handed to the SES SDK by the
// `email` worker Lambda. Phase 1 adds the first template (verification mail).
export {};
